coverage:
  status:
    patch: off

    project:
      default:
        informational: true
ignore:
  - "**/*.pb.go" # Ignore all auto generated protobuf structures.
  - "stability-tests" # Ignore the test code